import Link from "next/link";
import { ArrowRight, Braces, CheckCircle2, Code2, GitBranch, LockKeyhole } from "lucide-react";
import { Reveal } from "@/components/reveal";

export const metadata = { title: "Method" };

export default function MethodPage() {
  return <div className="page-wrap"><div className="eyebrow-row"><span className="micro-label">Method / 03</span><span className="form-hint">Read before you remix</span></div><div className="section-heading" style={{ marginTop: 35 }}><div><h1 style={{ margin: 0, fontSize: "clamp(52px, 8vw, 104px)", lineHeight: .88, letterSpacing: "-.08em" }}>A prompt is<br /><span style={{ color: "var(--orange)" }}>a beginning.</span></h1></div><p>Prompt Forge separates the idea, the compiler, the runtime, and the evidence. That makes a flashy demo inspectable instead of magical.</p></div><div className="flow-line" style={{ marginBottom: 55 }}><span>source record</span><b>→</b><span>brief</span><b>→</b><span>deterministic compiler</span><b>→</b><span>sandboxed runtime</span><b>→</b><span>revision + seal</span></div><div className="method-grid"><Reveal className="method-card"><span className="micro-label">01 / Evidence</span><h2>Sources stay attached.</h2><p>Official model pages, public repos, DEV articles, and indexed social posts enter as short attributed records. We keep the claim and the source URL beside the artifact.</p><div className="flow-line"><Braces size={16} /><span>model</span><span>repo</span><span>post</span></div></Reveal><Reveal className="method-card" delay={.06}><span className="micro-label">02 / Compiler</span><h2>No hidden magic call.</h2><p>The core experience uses four deterministic templates. A prompt changes the scene, palette, controls, and telemetry; it never asks an unavailable model key to finish the job.</p><div className="flow-line"><Code2 size={16} /><span>SVG</span><span>Canvas</span><span>Three.js</span></div></Reveal><Reveal className="method-card" delay={.12}><span className="micro-label">03 / Runtime</span><h2>Play is evidence.</h2><p>Every preview loads in a script-only iframe. Controls change state, resets work, and the artifact exposes a small telemetry object for QA and future agent calls.</p><div className="flow-line"><CheckCircle2 size={16} /><span>load</span><span>interact</span><span>reset</span></div></Reveal><Reveal className="method-card" delay={.18}><span className="micro-label">04 / Integrity</span><h2>Revisions leave a seal.</h2><p>Create, update, and retire events append a SHA-384 chain. Replay detects the first broken link and the export keeps the source trail beside the result.</p><div className="flow-line"><LockKeyhole size={16} /><span>canonical JSON</span><span>replay</span></div></Reveal></div><section className="section-block"><div className="section-heading"><div><span className="micro-label">The score</span><h2>Boring math<br />beats a magic number.</h2></div><p>The engine returns a score, itemized factors, a recommendation, and a seal. It is a prompt-readiness signal—not a claim about model quality.</p></div><div className="stat-row"><div className="stat-cell"><strong>30%</strong><span>Specificity</span></div><div className="stat-cell"><strong>25%</strong><span>Interaction contract</span></div><div className="stat-cell"><strong>20%</strong><span>Visual direction</span></div><div className="stat-cell"><strong>25%</strong><span>Constraints + evidence</span></div></div></section><div className="export-card"><span className="micro-label">Ready to test the loop?</span><h2 style={{ margin: "10px 0 0", fontSize: 30, letterSpacing: "-.06em" }}>Compile something you can play.</h2><div className="hero-actions"><Link className="primary-button" href="/build">Open composer <ArrowRight size={15} /></Link><Link className="secondary-button" href="/agent">Try the agent console <GitBranch size={15} /></Link></div></div></div>;
}
